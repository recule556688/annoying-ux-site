services:
  annoying-ux-site:
    image: ${IMAGE:?Set IMAGE to your GHCR image, e.g. ghcr.io/user/repo:latest}
    container_name: annoying-ux-site
    restart: unless-stopped
    ports:
      - "8080:80"
    # Uncomment and set your NPM network name if NPM runs in Docker on the same host:
    # networks:
    #   - npm_default

# networks:
#   npm_default:
#     external: true
